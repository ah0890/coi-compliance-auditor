# Nothing in this file is required for the MVP.
# The auditor never sends email -- it only drafts .eml files into output/emails/.

# Optional: the name used to sign drafted notices (defaults to "Risk Management").
# SENDER_NAME=Dana Whitfield

# Placeholders for a future "send" command. Unused today; there is no SMTP code
# anywhere in the project.
# SMTP_HOST=smtp.example.com
# SMTP_USER=coi-bot@example.com
# SMTP_PASSWORD=
