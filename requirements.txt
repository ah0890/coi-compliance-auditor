pdfplumber>=0.11
pypdf>=4.2
pandas>=2.2
openpyxl>=3.1
PyYAML>=6.0
rapidfuzz>=3.9
typer>=0.12
python-dotenv>=1.0
reportlab>=4.2
pytest>=8.0
python-dateutil>=2.9

# Optional OCR extras (not required for the MVP).
# Install these plus the Tesseract binary only if you need scanned PDFs:
#   pip install pytesseract pdf2image
